// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://cryenq.github.io',
    // base entfernt - Repository wird zu cryenq.github.io umbenannt
});
